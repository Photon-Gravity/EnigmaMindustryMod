package enigma.custom.stats;

import mindustry.world.meta.Stat;
import mindustry.world.meta.StatCat;

public class EniStats {
	public static Stat polymorphProduction = new Stat("polymorphProduction", StatCat.power);
	public static Stat polymorphConsumption = new Stat("polymorphConsumption", StatCat.power);
}
